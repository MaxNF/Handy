<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:id="@+id/main_options"
        android:checkableBehavior="single">

        <item
            android:id="@+id/contactsFragment"
            android:icon="@drawable/ic_contacts"
            android:title="@string/contacts_fragment_label" />

        <item
            android:id="@+id/navigation_app_settings"
            android:icon="@drawable/ic_settings_icon"
            android:title="@string/settings_fragment_label" />

        <item
            android:id="@+id/navigation_help_fragment"
            android:icon="@drawable/ic_help_icon"
            android:title="@string/help_fragment_label" />
    </group>

    <group android:id="@+id/other_options"
        android:checkableBehavior="none">
        <item
            android:id="@+id/navigation_rate_app"
            android:icon="@drawable/ic_review"
            android:title="@string/rate_app_label" />
        <item
            android:id="@+id/navigation_premium"
            android:icon="@drawable/ic_solid_star"
            android:title="@string/premium_fragment_label" />

    </group>
</menu>